projects:
  default:
    # Secret for remote control via rundctl. Omit to disable remote control.
    secret: "925u6ar525f2d47c16dd6045309de79326"

    # Webhook configuration.
    webhook:
      # Auto-detect provider based on headers and domain.
      provider: "auto"

      # Secret for webhook authentication.
      secret: "620f2796c68d4658a3a1fa086bf95317"

    # Deployment configuration.
    deploy:
      # Custom deployment script to run.
      exec: "sh::deploy_script.sh"